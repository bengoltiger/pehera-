{
  "name": "pehra-citizen-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "PEHRA Citizen APK — Bluetooth relay + SOS companion to the authority People screen",
  "scripts": {
    "sync": "cap sync android",
    "android:apk": "cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor-community/bluetooth-le": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}